app_identifier("com.motiontography.quippix")
apple_id(ENV["APPLE_ID"] || "fstop@motiontography.com")
team_id("8ZXNTAGJB8")

for_platform :ios do
  for_lane :beta do
    app_identifier("com.motiontography.quippix")
  end
end
