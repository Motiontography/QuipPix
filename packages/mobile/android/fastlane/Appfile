# CI: uses SUPPLY_JSON_KEY_DATA env var (set in GitHub Actions)
# Local: set json_key_file to your service account JSON path
json_key_file(ENV["SUPPLY_JSON_KEY_FILE"] || "path/to/google-play-service-account.json")
package_name("com.motiontography.quippix")
